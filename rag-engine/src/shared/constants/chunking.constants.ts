/**
 * Default chunk size for splitting transcripts during ingestion.
 */
export const DEFAULT_CHUNK_SIZE = 512;

/**
 * Default overlap size between consecutive chunks during splitting.
 */
export const DEFAULT_CHUNK_OVERLAP = 64;

/**
 * Default minimum character count for a valid chunk.
 */
export const DEFAULT_CHUNK_MIN_SIZE = 64;
