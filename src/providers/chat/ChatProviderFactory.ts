import { ChatProvider } from '@/core/contracts';

export class ChatProviderFactory {
  public static create(): ChatProvider {
    throw new Error('ChatProvider implementation not available yet (Phase 15)');
  }
}
