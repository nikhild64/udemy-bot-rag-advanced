/**
 * Ingestion layer
 * Responsible for processing documents (ZIP extraction, parsing, chunking, and embedding generation).
 * Future implementation phases will add document pipelines here.
 */
export {};
