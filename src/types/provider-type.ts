/**
 * Types of external provider integrations supported by the knowledge engine.
 */
export enum ProviderType {
  EMBEDDING = 'embedding',
  CHAT = 'chat',
  VECTOR_STORE = 'vector_store',
  TRANSCRIPT_PARSER = 'transcript_parser',
}
